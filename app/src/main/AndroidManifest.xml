<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.android.apps.translate">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> <!-- Dla overlay, jeśli potrzebne -->

    <queries>
        <package android:name="dev.davidv.translator" /> <!-- Dla visibility Offline-Translator na Android 11+ -->
    </queries>

   <application
        android:allowBackup="false"
        android:label="Google Translate Overlay"
        android:icon="@android:mipmap/sym_def_app_icon"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".TranslateActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Główny launcher, naśladujący Google Translate -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Sharing tekstu (analogiczne do Google) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Sharing obrazów (dla tłumaczenia z obrazu) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <!-- Bezpośrednie tłumaczenie via ACTION_TRANSLATE -->
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="android.intent.action.TRANSLATE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".copydrop.gm3.TapToTranslateActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:excludeFromRecents="true"
            android:theme="@style/WideDialogTheme"> <!-- Dla floating overlay -->
            <!-- Tłumaczenie zaznaczonego tekstu (Tap to Translate) -->
            <intent-filter android:label="@string/translate_text">
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <action android:name="android.intent.action.PROCESS_TEXT_READONLY" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>
</manifest>
